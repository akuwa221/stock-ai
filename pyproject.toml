[project]
name = "stock-ai"
version = "0.1.0"
description = "Stock analysis Streamlit app"
requires-python = ">=3.11"

dependencies = [
    "streamlit>=1.61.1",
    "yfinance",
    "pandas",
    "supabase",
    "requests",
]

[tool.uv]
package = false